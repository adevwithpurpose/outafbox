---
import Testimonials from "../landing/Testimonials.astro";

const { data } = Astro.props;
// data might contain 'headline', pass it if Testimonials accepts it,
// otherwise simply render the component.
---

<Testimonials />
